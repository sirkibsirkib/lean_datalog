-- Abstract rewriting: closures, confluence, normalisation. Split into
-- parts, each building on the last; importing this root brings in all of
-- them, so downstream files need not know about the division.

import LeanDatalog.Utils.Relation.Closure
import LeanDatalog.Utils.Relation.Confluence
import LeanDatalog.Utils.Relation.Normal
